

const Common_btn = ({text}) => {
  return (
    <button className="py-[24px] px-[55px] bg-black rounded-[5px] text-white cursor-pointer">
        {text}
    </button>
  )
}

export default Common_btn
